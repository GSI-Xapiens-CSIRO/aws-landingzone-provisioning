# AWS Landing Zone Configuration
# Copy this file to .env and update with your values

# ============================================================================
# REQUIRED CONFIGURATION
# ============================================================================

# Email addresses for core accounts (MUST BE UNIQUE)
LOG_ARCHIVE_EMAIL="log-apps@binomika.kemkes.go.id"
SECURITY_AUDIT_EMAIL="audit-apps@binomika.kemkes.go.id"

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================

# Organization name
ORG_NAME="BB BINOMIKA"

# AWS Region for Landing Zone deployment
# Recommended: Choose the region closest to your primary operations
AWS_REGION="ap-southeast-3"

# ============================================================================
# AWS CREDENTIALS
# ============================================================================
# Note: Alternatively, use 'aws configure' or IAM role-based authentication

AWS_ACCESS_KEY_ID="your-access-key-id"
AWS_SECRET_ACCESS_KEY="your-secret-access-key"
# AWS_SESSION_TOKEN="optional-session-token"

# ============================================================================
# HUB ACCOUNTS (Production)
# ============================================================================
AWS_PROFILE_HUB01="BGSI-TF-User-Executor-RSCM"
AWS_PROFILE_HUB01_EMAIL="rscm@binomika.kemkes.go.id"
AWS_PROFILE_HUB02="BGSI-TF-User-Executor-RSPON"
AWS_PROFILE_HUB02_EMAIL="rspon@binomika.kemkes.go.id"
AWS_PROFILE_HUB03="BGSI-TF-User-Executor-SARDJITO"
AWS_PROFILE_HUB03_EMAIL="rssardjito@binomika.kemkes.go.id"
AWS_PROFILE_HUB04="BGSI-TF-User-Executor-RSNGOERAH"
AWS_PROFILE_HUB04_EMAIL="rsngoerah@binomika.kemkes.go.id"
AWS_PROFILE_HUB05="BGSI-TF-User-Executor-RSJPD"
AWS_PROFILE_HUB05_EMAIL="rsjpd@binomika.kemkes.go.id"

# ============================================================================
# UAT ACCOUNTS (Staging/Testing)
# ============================================================================
AWS_PROFILE_UAT01="BGSI-TF-User-Executor-RSCM-UAT"
AWS_PROFILE_UAT01_EMAIL="stg-rscm@binomika.kemkes.go.id"
AWS_PROFILE_UAT02="BGSI-TF-User-Executor-RSPON-UAT"
AWS_PROFILE_UAT02_EMAIL="stg-rspon@binomika.kemkes.go.id"
AWS_PROFILE_UAT03="BGSI-TF-User-Executor-SARDJITO-UAT"
AWS_PROFILE_UAT03_EMAIL="stg-rssardjito@binomika.kemkes.go.id"
AWS_PROFILE_UAT04="BGSI-TF-User-Executor-RSNGOERAH-UAT"
AWS_PROFILE_UAT04_EMAIL="stg-rsngoerah@binomika.kemkes.go.id"
AWS_PROFILE_UAT05="BGSI-TF-User-Executor-RSJPD-UAT"
AWS_PROFILE_UAT05_EMAIL="stg-rsjpd@binomika.kemkes.go.id"

# ============================================================================
# NOTES
# ============================================================================
# 1. Each account requires a unique email address
# 2. You can use email aliases (e.g., aws+logs@domain.com, aws+security@domain.com)
# 3. Ensure you have access to these email addresses for account verification
# 4. The script must be run from the AWS Management Account
# 5. Requires AWS CLI v2 and jq to be installed